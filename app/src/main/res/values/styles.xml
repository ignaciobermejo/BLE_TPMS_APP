<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.DayNight.ActionBar">
        <!-- android:textColorPrimary is the color of the title text in the Toolbar  -->
        <item name="android:textColorPrimary">@color/black</item>
        <!-- actionMenuTextColor is the color of the text of action (menu) items  -->
        <item name="actionMenuTextColor">@color/black</item>
        <!-- Tints the input fields like checkboxes and text fields -->
        <item name="colorAccent">@color/amber_600</item>
        <!-- Applies to views in their normal state. -->
        <item name="colorControlNormal">@color/black</item>
<!--        &lt;!&ndash; Applies to views in their activated state (i.e checked or switches) &ndash;&gt;
        <item name="colorControlActivated">@color/amber_600</item>
        &lt;!&ndash; Applied to framework control highlights (i.e ripples or list selectors) &ndash;&gt;
        <item name="colorControlHighlight">@color/amber_600</item>-->

        <!-- Enable these below if you want clicking icons to trigger a ripple effect -->
        <!--
        <item name="selectableItemBackground">?android:selectableItemBackground</item>
        <item name="selectableItemBackgroundBorderless">?android:selectableItemBackground</item>
        -->
    </style>

    <style name="MainToolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/black</item>
    </style>

    <!--<style name="ThemeOverlay.App.Card" parent="">
        <item name="colorPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/amber_600</item>
    </style>-->

    <style name="ShapeAppearance.App.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">cut</item>
        <item name="cornerSize">10dp</item>
    </style>

    <style name="VehicleCard.TextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="VehicleCard" parent="Widget.MaterialComponents.CardView">
        <!--<item name="materialThemeOverlay">@style/ThemeOverlay.App.Card</item>-->
        <item name="shapeAppearance">@style/ShapeAppearance.App.MediumComponent</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <!--<item name="cardCornerRadius">20dp</item>
        <item name="cardBackgroundColor"> @color/amber_600_alpha</item>-->
        <item name="cardBackgroundColor"> @color/amber_600_alpha</item>
        <item name="cardElevation">0dp</item>
        <item name="strokeColor">@color/amber_600</item>
        <item name="strokeWidth">4dp</item>
        <item name="rippleColor">@color/amber_600</item>
        <item name="cardPreventCornerOverlap">false</item>
    </style>

    <style name="VehicleCard.Large" parent="VehicleCard">
        <item name="android:layout_width">150dp</item>
        <item name="android:layout_height">150dp</item>
        <item name="android:layout_margin">20dp</item>
    </style>
    <style name="VehicleCard.Medium" parent="VehicleCard">
        <item name="android:layout_width">120dp</item>
        <item name="android:layout_height">120dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>
    <style name="VehicleCard.Small" parent="VehicleCard">
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>
</resources>